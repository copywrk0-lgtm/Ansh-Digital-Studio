import Image from 'next/image';import {portfolio} from '@/data/portfolio';export function GalleryGrid(){return <section className='galleryGrid section'>{[...portfolio,...portfolio].map((p,i)=><figure key={i}><div><Image src={p.image} alt={p.title} fill className='cover'/></div><figcaption><span>{String(i+1).padStart(2,'0')}</span><strong>{p.type}</strong></figcaption></figure>)}</section>}
