import {Eyebrow} from '@/components/ui/Eyebrow';
import {site} from '@/lib/site';

export function CtaSection(){return <section className='cta section'><Eyebrow light>Check your date</Eyebrow><div><div><h2>One message is enough<br/><em>to get started.</em></h2><p>Send the date, venue and function type. We’ll confirm availability and share the most suitable photography or photo + video options.</p><div className='ctaChips'><span>{site.rating}★ Google rating</span><span>{site.reviewCount} reviews</span><span>Photo + video</span></div></div><div className='ctaPanel'><span className='ctaPanelLabel'>QUICKEST WAY TO ENQUIRE</span><a className='primaryBtn' href={site.whatsapp} target='_blank' rel='noreferrer'>Get availability & package details ↗</a><a className='secondaryBtn' href={`tel:${site.phone}`}>Call {site.phoneLabel}</a><small>Send: wedding / function type · date · venue / city · whether you need photography, video or both.</small></div></div></section>}
