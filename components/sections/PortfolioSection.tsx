import Image from 'next/image';import {portfolio} from '@/data/portfolio';import {Eyebrow} from '@/components/ui/Eyebrow';export function PortfolioSection(){return <section className='work'><div className='workIntro section'><Eyebrow light>Selected stories</Eyebrow><h2>Not poses.<br/><em>Real presence.</em></h2></div>{portfolio.map((p,i)=><article className={`project ${i%2?'reverse':''}`} key={p.n}><div className='projectImage'><Image src={p.image} alt={p.title} fill className='cover'/></div><div className='projectMeta'><span>{p.n} · {p.type}</span><div><h3>{p.title}</h3><p>{p.desc}</p></div></div></article>)}</section>}
