'use client'; import Link from 'next/link'; import {useEffect,useState} from 'react'; import {navItems} from '@/data/nav'; import {site} from '@/lib/site'; import {MobileMenu} from './MobileMenu'; export function Header(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>24);f();addEventListener('scroll',f,{passive:true});return()=>removeEventListener('scroll',f)},[]);return <><header className={`nav ${scrolled?'navScrolled':''}`}><Link className='brand' href='/'><span className='brandMark'>A</span><span>ANSH DIGITAL</span></Link><nav className='desktopNav'>{navItems.slice(0,4).map(i=><Link key={i.label} href={i.href}>{i.label}</Link>)}</nav><a className='navCta' href={site.whatsapp} target='_blank' rel='noreferrer'>Check availability ↗</a><button className='menuButton' aria-label='Toggle menu' onClick={()=>setOpen(!open)}><span/><span/></button></header><MobileMenu open={open} onClose={()=>setOpen(false)}/></>}
